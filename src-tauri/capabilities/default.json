{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main + print windows",
  "windows": ["main", "print"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
