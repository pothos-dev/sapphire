.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri build artifacts
/src-tauri/target
/src-tauri/gen

# Playwright artifacts (keep tests/screenshots/, which is committed)
/test-results
/playwright-report
/.playwright

# Local-only example OKF bundle (Google ga4 sample); not shipped in the repo
/examples

